import { FC } from 'react';
import XmsHeader from '@/components/xms-header';

const AuthList: FC = () => {
  return (
    <div>
      <XmsHeader title='权限列表' />
    </div>
  );
};

export default AuthList;
